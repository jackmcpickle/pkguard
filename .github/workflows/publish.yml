name: Publish

on:
  push:
    branches: [main]
    paths:
      - "crates/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".versionrc.json"
      - "scripts/cargo-version-updater.cjs"
      - "scripts/cargo-lock-updater.cjs"
      - "scripts/catalog-version-updater.cjs"
      - ".github/workflows/publish.yml"
      - ".github/workflows/release.yml"
  workflow_dispatch:

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  bump:
    if: ${{ !contains(github.event.head_commit.message, '[skip publish]') && !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      released: ${{ steps.release.outputs.released }}
      version: ${{ steps.release.outputs.version }}
    steps:
      # Deploy key (ruleset bypass) so the release commit/tag can push to main.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ssh-key: ${{ secrets.DEPLOY_KEY }}

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Test
        run: cargo test --workspace

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Bump version, changelog, and tag
        id: release
        run: |
          set -euo pipefail
          if ! npx --yes commit-and-tag-version@13; then
            echo "No releasable commits since last tag; skipping release."
            exit 0
          fi
          version="$(node -e "const fs=require('fs'); const u=require('./scripts/cargo-version-updater.cjs'); process.stdout.write(u.readVersion(fs.readFileSync('Cargo.toml','utf8')))")"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "released=true" >> "$GITHUB_OUTPUT"

      # Release commits are pushed with a deploy key and produce no workflow
      # runs, so CI never inspects them. Verify the catalog here instead, before
      # anything lands — a stale catalog would otherwise fail the next
      # unrelated PR.
      - name: Docs catalog is fresh
        if: steps.release.outputs.released == 'true'
        run: |
          cargo run -q -p pkguard -- dump-catalog > site/src/generated/catalog.json
          git diff --exit-code site/src/generated/catalog.json

      - name: Push release commit and tag
        if: steps.release.outputs.released == 'true'
        run: git push --follow-tags origin HEAD:main

  cut-release:
    needs: bump
    if: needs.bump.outputs.released == 'true'
    uses: ./.github/workflows/release.yml
    with:
      tag: v${{ needs.bump.outputs.version }}
    secrets:
      HOMEBREW_TAP_DEPLOY_KEY: ${{ secrets.HOMEBREW_TAP_DEPLOY_KEY }}
      CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
    permissions:
      contents: write

